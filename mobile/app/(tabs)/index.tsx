import { View, Text } from "react-native";

export default function TabIndex() {
    return (
        <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
            <Text>Tabs Home (later: dashboard)</Text>
        </View>
    );
}